import React from "react";

const notes = () => {
  return (
    <div>
      {/* {isLoading && <h2>Loading...</h2>}
      {isError && <h2>{error.message}</h2>}
      {data?.data.map((user) => (
        <main key={user.id}>
          <h3 style={{ color: "darkblue" }}>
            {user.id}. {user.name}
          </h3>
        </main>
      ))}
      {isInitialLoading ? <span>Loading...</span> : <span>Not ready ...</span>} */}
    </div>
  );
};

export default notes;
